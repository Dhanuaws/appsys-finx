============================================================
       LAMBDA FUNCTION - TERRAFORM CONFIGURATION INPUT
============================================================

Please fill in the details below and share it back.
Leave any optional field blank if not needed — defaults will be used.


------------------------------------------------------------
                    REQUIRED INFORMATION
------------------------------------------------------------

1. Function Name        : 
   (e.g., email-parser-lambda)

2. Runtime              : 
   (e.g., python3.12, python3.11, nodejs20.x, nodejs18.x, java21, dotnet8)

3. Handler              : 
   (e.g., lambda_function.lambda_handler for Python, index.handler for Node.js)

4. AWS Region           : 
   (e.g., us-east-1, ap-south-1, eu-west-1)


------------------------------------------------------------
                   OPTIONAL INFORMATION
------------------------------------------------------------

5. Trigger / Event Source : 
   (e.g., API Gateway, S3, SES, SQS, SNS, CloudWatch Schedule, EventBridge)

6. Memory (MB)            : 
   (Default: 128 | Options: 128, 256, 512, 1024, 2048, etc.)

7. Timeout (Seconds)      : 
   (Default: 3 | Max: 900)

8. Environment Variables  : 
   (List as key=value pairs, one per line)
   - 
   - 
   - 

9. VPC Required? (yes/no) : 
   If yes:
     - VPC ID            : 
     - Subnet IDs        : 
     - Security Group IDs: 

10. IAM Permissions        : 
    (What AWS services does the Lambda need access to?)
    (e.g., S3 read/write, DynamoDB read, SES send, CloudWatch Logs)
    - 
    - 
    - 

11. Tags                   : 
    (List as key=value pairs)
    - Environment = 
    - Project     = 
    - Owner       = 

12. Lambda Layers          : 
    (ARNs of any Lambda layers to attach)
    - 
    - 

13. Source Code Path        : 
    (Local path to your Lambda source code folder or ZIP file)
    (e.g., ./src or ./lambda.zip)


============================================================
